import 'package:flutter/material.dart';

class MotorComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Motor',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
